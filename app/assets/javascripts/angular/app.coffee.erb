@csc = angular.module('csc',['smart-table'])
<% url = Csc::Application.routes.url_helpers %>
@urls = 
  clients_path: '<%= url.references_clients_path(format: :json) %>'
  models_path: '<%= url.references_models_path(format: :json) %>'
  orders_path: '<%= url.documents_orders_path(format: :json) %>'
